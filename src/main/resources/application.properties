# Configuração do Banco de Dados PostgreSQL
spring.datasource.url=jdbc:postgresql://postgres.railway.internal:5432/railway
spring.datasource.driverClassName=org.postgresql.Driver
spring.datasource.username=postgres
spring.datasource.password=anWKRGSHifsHTBiATprceTddCeugZhSP
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

# Dialeto do Hibernate para PostgreSQL (corrigido)
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Exibir as queries SQL no console (útil para debugging)
spring.jpa.show-sql=true

# Configurações de Conexão com o HikariCP (pool de conexões)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.max-lifetime=600000

# H2 Console (Remover ou comentar se você não usar o H2)
# Caso não utilize o H2, as próximas duas linhas podem ser removidas ou comentadas
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console

# Configurações de transação do Hibernate
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true

# Configuração do pool de conexões do PostgreSQL
spring.datasource.test-on-borrow=true
spring.datasource.validation-query=SELECT 1
spring.datasource.validation-query-timeout=3
